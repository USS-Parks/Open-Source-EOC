{
  "name": "@openeoc/root",
  "version": "0.0.0",
  "private": true,
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "check": "pnpm -r exec tsc --noEmit && eslint . && node scripts/license-scan.mjs && node scripts/check-links.mjs && node --test deploy/windows/desktop.test.mjs deploy/windows/installer/installer.test.mjs && vitest run",
    "lint": "eslint .",
    "license-scan": "node scripts/license-scan.mjs",
    "check-links": "node scripts/check-links.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.10.0",
    "typescript": "^5.5.0",
    "typescript-eslint": "^8.70.0",
    "vitest": "^5.0.1"
  }
}
