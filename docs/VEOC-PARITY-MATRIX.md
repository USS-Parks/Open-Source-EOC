# Open-Source-EOC parity capability matrix

Source-backed inventory of the assessed capabilities (VEOC-79E), reconciled
for the release decision on 2026-09-25 through `98f050a` ("Veoci and air gap
follow-up: Vitest collected a node test file"), the commit after every unit of
the Veoci Integration and Air Gap PSPR had landed, against the receipts in the
[V1 ledger](./process/V1-LEDGER.md), which are cited by their heading.
The remaining blockers and the release decision are in
[RELEASE-DECISION.md](../RELEASE-DECISION.md). The
internal run of the [WebEOC side-by-side evaluation](./WEBEOC-SIDE-BY-SIDE.md)
is cited as "the side-by-side run". Each row ties
a canonical obligation or an assessed gap to the reference product it mirrors,
the primary source for that reference, the operator behavior expected, the
local implementation and its evidence, a current status, and the owner of
what remains.

This matrix reconciles [FACET-STATUS.md](./FACET-STATUS.md): its `verified`
labels recorded earlier implementation gates, not demonstrated commercial-
product parity. Where local behavior is a completed increment but not full
parity, the status here is `partial` with the owner named. Historical
receipts in [VEOC-EXECUTION-LEDGER.md](./process/VEOC-EXECUTION-LEDGER.md) are not
rewritten; overbroad status claims are corrected here, not erased there.

## Scope, exclusions, and rules

- **Public access is an intentional exclusion.** The platform is For Official
  Use Only; there is no anonymous or public-facing facet. Access is binary:
  members and authorized mutual-aid guests view their authorized incident and
  dashboard with role-based field restrictions; everyone else is denied.
  VEOC-80 database and browser evidence is integrated (`dac0667`). An
  administrator may lock one incident, which refuses guest reads of its boards
  at the row-level security wall; lockdown is off by default and never
  automatic ("V1 W4.9: incident lifecycle").
- **Every included gap has an owner**: Basho for an external input or a
  decision, or "open engineering" for work no executed unit owns. A claim with
  no supporting evidence is recorded as `unknown`, not asserted.
- **A `verified` status** means a CI-green implementation gate was met and the
  behavior is not known to fall short of the referenced product at the depth
  assessed. It is not a commercial-parity certification. Live-pilot,
  real-hardware and disconnected-transfer gates are external inputs, listed
  with what each unblocks in the release decision.

## Reference product key

| Key | Reference | Primary source |
|---|---|---|
| WebEOC-core | Juvare WebEOC (Nexus) core: boards, incidents, sharing, activity log | https://docs.juvare.com/webeoc-onnexa/ |
| WebEOC-inc | WebEOC incident model and incident-scoped board data | https://docs.juvare.com/webeoc-onnexa/help/incidents/about-incidents.htm |
| WebEOC-Maps | WebEOC Maps add-on (optional module) | https://docs.juvare.com/webeoc-onnexa/ |
| WebEOC-DS | WebEOC DesignStudio board authoring (optional module) | https://docs.juvare.com/webeoc-onnexa/ |
| Juvare-other | Separate Juvare products (e.g. eICS, EMResource, EMTrack), not WebEOC core | https://www.juvare.com/ |
| Esri-EMO | Esri Emergency Management Operations solution | https://doc.arcgis.com/en/arcgis-solutions/latest/reference/use-emergency-management-operations.htm |
| Veoci | Veoci virtual EOC platform: rooms, plans, solution packages, workflows, forms and notifications | https://veoci.com/downloadable/veoci-whole-community-capabilities-list/ |

Distinguishing the reference matters: capabilities that are separate Juvare
products or optional WebEOC modules are not part of WebEOC core and are marked
accordingly, so parity against "WebEOC" is not overstated. Veoci rows follow
the [Veoci platform research](./process/VEOCI-PLATFORM-RESEARCH-2026-09-24.md)
and cite its candidate numbers (VC-nn).

## Facets (F1-F20)

| ID | Capability | Reference | Operator behavior | Local implementation / evidence | Status | Owner |
|---|---|---|---|---|---|---|
| F1 | Versioned board schema, input + display views | WebEOC-core / WebEOC-DS | Admin defines a board; operators enter and view records | Board schema + runtime (VEOC-09/10); declarative layouts and upgrade preflight (`b7cb680`); operational record workspace (`32249fc`); structured designer, preview, immutable publication and safe apply (`75464fc`). Receipts "V1 W4.1 part one: board engine depth" and "V1 W4.1 part two: board screen controls" add record-level access, archive, restore and tombstone delete, per-record change history, CSV and Excel import and export, relative-date and set operators, multi-key sort and grouped views, each on real PostgreSQL and in Chrome; "V1 W4.2: views beyond the list" adds kanban, calendar and chart modes; "V1 W4.12: REST record writes through the sync log" puts console writes on the live sync log; "V1 W4.4: WebEOC migration" imports WebEOC board records. The side-by-side run publishes a template on the Templates screen, which creates its board, then enters, edits, groups, exports and reads the history of its records. "V1 W4.13: gaps the WebEOC side-by-side run found" adds "Create a board from a published template" on Templates and shows a new board in Boards and the pickers without a reload; "V1 W4 milestone gate" makes the designer show the applied version after publish and apply. "Readiness RD8: the screens on the open engineering list" shows choice labels in the record detail, change history, kanban cards and calendar, lets an administrator of the jurisdiction who is not an instance administrator create boards from published templates, saves conditions, sort keys and a group field into a template's own views, and retitles boards activated under the template key; "Operator trust TP4: durable work and explicit state" restores a record draft against the server's current values and names a field both sides changed; "Operator trust TP5: information state you can read" says why a table is empty. "Veoci and air gap VA9 part one: the signature field" adds a signature field drawn on a pad, stored as the jurisdiction's file with the signer and time, and written in words in tables, history and exports; "Veoci and air gap VA15: workflow guards and per-state field permissions" keeps a field read-only in a workflow state through the edit form, REST and sync, for administrators too; "Veoci and air gap VA25: declarative action catalog" (VC-17) lets a template set a field, create a linked record, request a transition or send an in-app notice when a record is created, a field changes or a state is entered, run as the person whose write set it off, with loop and depth limits; "Veoci and air gap VA17 completion: board record import reports" keeps a report of each board record import; "Veoci and air gap VA26: all-or-any conditions, date and text functions, every view option in the designer" (VC-18) lets a view, a guard and an action condition hold on all or any of its conditions, with groups one level deep, adds case-blind text equality and day operators (on, before, after and between a day such as today plus or minus days; within the last or next N days) counted in a time zone stored with the set, and puts every view option (columns and their order, all or any, sort keys, group field, zone) on the designer's Views tab, its real-database tests holding the server's SQL to the shared evaluation; "Veoci and air gap follow-up: sync edits set board actions off" runs a board's actions for a record created or changed through the sync hub, an offline edit delivered when its device reconnects included, once and as the person who made the edit, by real-database tests with devices on the sync route. VA9, VA15, VA25 and VA26 are proven by real-database, component and browser tests at 1586 by 992 and 1534 by 790, the import report by real-database tests. Not claimed: form submissions, imports and records from a federation peer set off no action; condition groups deeper than one level | verified | - |
| F2 | Position login + immutable activity/position logs | WebEOC-core | Sign into a position; every action is attributed and logged | Positions, sign-in, append-only audit (VEOC-07/11); incident-position attribution extended (`ca0d130`); "V1 W3.1: audit chronology" gives the chronology a filterable, paged, exportable screen with attributed corrections, and "V1 W3.0: administration" audits guest grants and position assignment; "V1 W2.9: retention and export" exports the audit trail as CSV and signed JSON and never purges it; "V1 W2.10: MFA" adds two-step sign-in; "V1 W4.1 part one: board engine depth" adds per-record history with who, position and values before and after; "V1 79D+D33 part one: the integrated cross-boundary exercise" keeps a partner's author and position in the record, the sync log and the audit after the partner is revoked; "V1 W2.12: network calls out of every write path" audits notification rule creation, the one administrative write found unaudited; "Partner sharing PS2: incident positions and partner authors" names a partner author's incident position and organization on the owner's record detail and history, by the grant in effect when it wrote, and "Partner sharing PS4: incident-wide threads" names each message's sender organization; "Operator trust TP1: request lifecycle and findability" records who accepted a request and as which position; "Readiness RD9 part one: workflow, staffing and facilities" names each actor on the workflow history; "Veoci and air gap VA37 part one: ICS forms as stored components" keeps each form version's preparer, role label and organization; "Veoci and air gap VA25: declarative action catalog" audits every action run as the person whose write set it off, with its chain and outcome; "Veoci and air gap VA20: exchange by file" audits a batch imported by file under the administrator who imported it; "Veoci and air gap VA32: OpenAPI document and scoped service identities" audits a service identity's writes under its own backing row, named `<name> (service identity)`, never a person | verified | - |
| F3 | Store-and-forward federation, local replication | WebEOC-core | Agencies share boards across a federation boundary | Federation service and sharing agreements (VEOC-30). "V1 W2.1: outbound delivery queue" delivers the outbox to a linked peer through a partition; "V1 W3.6: federation and peers" adds the screen; "V1 W3.11: engine gaps the screens exposed" queues shared-board sync edits automatically with no echo; "V1 W4.12: REST record writes through the sync log" forwards console writes of records with no incident; "V1 79D+D33 part one: the integrated cross-boundary exercise" federates a record to a second instance with the same record id, attributed there to "Valley City EOC", with the incident report kept home. "Readiness RD10: federation of record deletes and earlier records" forwards the delete of a record with no incident to every peer that reads the board, where a delete wins over edits and an edit that reaches a deleted record is listed as a conflict, and sends a board's jurisdiction-wide records to a peer when an agreement is made, audited as "federation.backfilled", on two instances on real PostgreSQL. "Veoci and air gap VA2: federation batch sizing" cuts batches to 768 KiB in queue order under an explicit 8 MiB receive limit, so a backfill over 3 MiB and a backlog held through a simulated day's partition arrive in parts and in order; "Veoci and air gap VA19: signed peer identity" signs every batch with the sending instance's Ed25519 key, refuses an unsigned or forged batch before anything in it applies, and revokes an agreement so the revoking instance neither sends nor accepts the board; "Veoci and air gap VA20: exchange by file" carries the same signed batches on removable media between two instances with no network path, in both directions, with a receipt signed by the receiver and a repeated import changing nothing. VA2 and VA19 are proven on two instances over HTTP on real PostgreSQL, VA20 on two instances with no network path between them, and VA19 and VA20 also by browser tests at both viewports. Remaining, as the RD10 receipt states: records of an incident, their edits and their deletes stay on their home instance and are not backfilled, because a partner applies what it receives to its board's jurisdiction-wide document; doing it safely needs an opt-in on the agreement and the partner's copy kept under an incident of its own, a design for Basho to choose (the Veoci roster's decision 3 keeps it unbuilt). Also bounded: the peer attributes a batch received over the network to the sending instance, not the author, and a batch imported by file to the administrator who imported it; a sharing agreement federates a restricted board's live edits, which the record rule does not govern ("V1 W4.1 part one: board engine depth"). From VA19 and VA20: the partner is not told of a revocation; resource escalation and JIC approval deliveries are not signed; there is no replay sequence (a replayed batch changes nothing); the instance key has no rotation on screen; batch files are signed, not encrypted; an existing peering receives nothing after the upgrade until both administrators record each other's keys. Not run: the disconnected drill's day-2 exchange by file on two real hosts, which is Basho's | partial | Basho: accept incident records as a v1 limit or choose the opt-in design (then open engineering); the drill's day-2 exchange (a run) |
| F4 | Board-triggered notifications, webhooks, multi-channel | WebEOC-core | A record change fires a notification/webhook | Notify + webhook delivery (VEOC-14) and governed workflow notifications (`6c8dddc`). "V1 W2.1: outbound delivery queue" moves every external send out of the write path into a durable outbox with retry, dead letter and circuit breaking, run by the scheduler ("V1 W2.2: scheduler"), which the Windows network host runs as a service ("Readiness RD3: the Windows network host") and whose election with its six jobs an installed demo profile logs ("Readiness RD11: the remaining checks"; each job's run is not logged); "V1 W2.7: threat-model controls" adds the allowlist and per-rule rate caps; "V1 W4.0 part one: email and SMS channels" adds SMTP email and SMS; "V1 W3.12: screens for the remaining operator routes" authors rules on screen; "V1 W4.0 part two: contacts and mass notification" adds contacts, groups, mass notification with delivery receipts and call-down escalation; "V1 W4.13: gaps the WebEOC side-by-side run found" lists, pauses, changes and removes rules, audited, and writes messages in field and value labels; "V1 W2.12: network calls out of every write path" audits rule creation. Channels: in-app, webhook, push (ntfy), email and SMS. The side-by-side run fires an email and an SMS from a record change. "Veoci and air gap VA1: hold, do not drop" holds a delivery that cannot reach its route for its channel's window (72 hours unless an administrator sets another), shows it as "Waiting for a route", then "Expired, not sent" with **Resend**, and sends scheduled report email through the same queue with retry; "Veoci and air gap VA7: activation notifies; people reached by group, position and shift" (VC-02, VC-15) addresses mass sends and rules to contact groups, positions and whoever is on shift, falls back from a text to email when the text is not acknowledged, and notifies chosen people at activation; "Veoci and air gap VA8: response options on mass sends" (VC-03) asks up to six answers on the acknowledgement page and counts them; "Veoci and air gap VA21: local carriers" sends SMS through an Android phone's SIM running SMS Gateway for Android on the site network, reads text replies back as acknowledgements and answers, prints a call-down sheet whose acknowledgements are entered afterward, and adds a radio and runner log board. Each by real-database and browser tests against local stand-ins (a relay, a fixture provider, a fixture phone for the gateway). Not claimed: a voice channel; Teams or Slack as a rule channel (the webhook body is not a chat message format); a live SMTP relay or SMS provider, which waits on the Finish PSPR's section 7 item 6; a real phone with a SIM, which is Basho's; a GSM modem, which needs a bridge no unit built; the phone's later delivery state, which is not read back, so "sent" means the phone took the text; pausing a rule does not re-check the webhook allowlist | verified | Basho: a bridge for a GSM modem, or the phone as the local carrier (decision 6) |
| F5 | ICS forms, IAP builder, 213RR lifecycle | WebEOC-core | Build ICS forms and the operational-period IAP; run resource requests | Forms/IAP/resource lifecycle (VEOC-34/35); IAP now incident-context driven (`8c96103`); "V1 W3.8: JIC and resources completion" wires costs, cost export and mutual-aid escalation; "V1 W4.8: resources" adds NIMS resource typing, a pool with demobilization and a cost rollup; "V1 79D+D33 part one: the integrated cross-boundary exercise" assigns an owner's request on the incident to a partner as supplier and approves the plan; "Partner sharing PS1: shared incident requests" shares every request on an incident with every organization on it (costs stay with the owner), lets the assigned partner record the delivery steps, and lets a partner request from the incident's owner, who triages and assigns it, as Basho decided for request ownership in the partner sharing plan (decision 8). "Operator trust TP1: request lifecycle and findability" separates receipt from acceptance, records who accepted a request, shows its owner and next action, asks a reason to decline or cancel, and finds a request by number or words; "Operator trust TP2: my work" takes a request's steps from the My work queue; "Readiness RD9 part two: resources" caps assignments at a request's quantity, adds per-resource history, changes a pool resource's kind or type while it is not assigned, edits and deletes local kinds, and receives a peer's repeated escalation once. "Veoci and air gap VA37 part one: ICS forms as stored components" keeps ICS 201, 202, 203, 204, 205, 205A, 206, 207, 208, 209, 211, 213, 214, 215 and 215A as components of an operational period, laid out after the NIMS ICS Forms Booklet (FEMA 502-2, September 2010), each prefilled from the incident's records as the IAP reads them, edited field by field, attributed, versioned, marked draft or ready and printed alone; "Veoci and air gap VA37 part two: the IAP assembled from its forms" assembles the period's IAP from the ready components the planning section chooses, approves it as a whole, and turns a form changed after approval into the plan's next revision with the approved one kept; "Veoci and air gap VA38: the ICS 213RR as a form component" renders a resource request as its ICS 213RR through acceptance, sourcing, assignment and deployment, prints it, and holds it with the period's forms and in the IAP; "Veoci and air gap VA9 part one: the signature field" lets a jurisdiction add a signed approval to its 213RR board. Each by real-database, component and browser tests at both viewports. These close the earlier not-claimed items for objectives and the ICS-208 safety message on screen and for an ICS-205 with content, which is now written as a form. Not claimed: one current operational period per incident ("V1 W6.5: training kit"); the ICS-201, ICS-211 and ICS-215, assembled or prefilled, read the Resource Requests and Sign In/Out boards, not Resources requests or Staffing check-ins (`gatherContext` in `server/src/iap/service.ts`), and the 205's prefill needs a radio communications board; the 206's medical facilities and the 208's safety message start empty, since the server's incident context carries neither; a plan in approval keeps the forms it was submitted with until a refresh after approval; guests cannot read the resource module, and another organization's reader sees a 213RR's finance blocks empty | verified | - |
| F6 | Any board as a live geospatial layer; field-to-COP loop | Esri-EMO / WebEOC-Maps | Drop a record on the map; it appears as a COP layer | VEOC-79B1/B2 deliver incident-tagged field capture, scoped reads/counts and partner isolation; VEOC-79C2 connects persisted datasets to COP layers; P-COP and D29 provide PostgreSQL/Chrome map inspection and field-capture evidence (`e20e772`, `d1c5d63`); "V1 W4.5: operational vector tiles" draws boards past one client page as tiles; "V1 W4.7: field depth" captures lines and polygons; "V1 79D+D33 part one: the integrated cross-boundary exercise" reconciles a partner's shelter dataset between the COP layer, the impact indicator and a direct PostGIS count, and fixes a partner's field impact posted from the map; "V1 79D+D33 part two: the integrated visual and accessibility review" keeps another incident's boards out of the map layer list; "Operator trust TP9: from the map to the action" opens a selected record's status, source board, observation time and last change, with who made it, beside the map, and links the record in its board and the map both ways | verified | - |
| F7 | Offline XLSForm-compatible smart forms | Esri-EMO (Field Maps/Survey123) | Fill a form offline; sync later | Smart forms + XLSForm subset (VEOC-22) and durable field reconciliation (`d1c5d63`). "V1 W4.7: field depth" adds line and polygon capture, barcode, photo and audio questions, cascading selects and repeats in the runner, the XLSForm importer and the field screen, with photo and audio queued offline and uploaded after the record synchronizes, proven on real PostgreSQL and in Chrome online and offline; "V1 W3.10: settings and field reports" adds the Field Reports screen; "V1 W4.10: progressive web app" installs the app and keeps the shell and a saved session through a lost connection; "Readiness RD2 part three: the first review" makes Field Reports a triage screen with Verify and Mark unverified; "Readiness RD5: the air gap" opens the console with no connection after a restart. "Veoci and air gap VA22: field breadth" queues map points on an incident's boards, messages to incident threads and an administrator's new tasks on the device, delivers messages, tasks and task completions once through an exactly-once route, syncs a board with record rules per record on an incident, and keeps work that reaches an incident closed meanwhile as a late submission for the owner's administrators to accept or refuse, proven by real-database tests and by browser tests offline and back at both viewports; "Veoci and air gap VA9 part one: the signature field" draws an XLSForm signature question on the pad and queues it with the report; "Veoci and air gap VA9 part two: QR codes on badges and pool resources" reads a QR code from a photo in a browser with no barcode detector, with the decoder precached for offline use; "Veoci and air gap VA26: all-or-any conditions, date and text functions, every view option in the designer" runs XLSForm's `contains()` and `starts-with()`, which the importer refused before; "Veoci and air gap VA34: offline device PIN for shared devices" (VC-27) lets a person seal what the device keeps for them under a PIN, and a report queued offline stays queued through a restart under the PIN and reconciles once the PIN opens it, by unit, component and browser tests on the real build (see G-PWA). Not claimed: `range`, `or_other` and `repeat_count`, which the importer refuses by row; live-video barcode scanning, and Code 128 or Code 39 in a browser without its own detector; in-page audio recording; a continuous GPS trace; repeats as child records; offline queueing for a board with record rules outside an incident; a map point saved online whose answer is lost arrives a second time on reconcile; an incident board's form opens offline only after the device has shown that board on the map with a connection; more than 10 MB per queued file or 50 MB per person per incident on the device; installing on a phone, whose procedure is written ("Veoci and air gap VA23: phones on a host's authority") and not walked | verified | - |
| F8 | FEMA doctrine as schema: Lifelines, PDA outputs | Esri-EMO | Report lifeline status; produce PDA/declaration outputs | VEOC-20/23 provide eight Lifelines, immutable SITREPs, approved-assessment aggregation and declaration support. D13 and P-LIFE-1 through P-LIFE-4 add attributed assessments, independent ESF state, conflicts, actions, history and operational relationships. "V1 W3.2: damage assessment" gives intake moderation, loss summary, thresholds and the declaration summary a screen; "V1 W4.11: Public Assistance and shelter census" adds the PA inventory by FEMA categories A to G, the per-capita indicator from PA cost, and a shelter census from facilities reports in the declaration summary, proven on real PostgreSQL and in Chrome; "Operator trust TP5: information state you can read" shows when a lifeline's condition was observed apart from when the server received the report. "Veoci and air gap VA14: Public Assistance force account" (VC-10), with "Veoci and air gap VA13 and VA14 corrections: review findings", costs labor per person per local day from closed check-ins and uncovered shifts (overlaps merged, overtime past a daily threshold, fringe added, cents rounded half up) and equipment hours at a jurisdiction's imported rate schedule, lays them out as FEMA's Force Account Labor and Equipment Summary Records (Forms 009-0-123 and 009-0-124), and rolls the total into one Public Assistance line item, by real-database, component and browser tests with hand-worked figures. The PA guide is now named in the dictionary: FP 104-009-2, Version 5.0 as amended (decision 10). Not claimed: the census where the facilities integration is off (the summary says it is not available); the PA guide edition, which Basho has not confirmed; eligibility, weekly overtime, materials, rented equipment and contract work; FEMA's equipment rates, which ship empty with an import path (decision 11); volunteer hours as donated resources ("Veoci and air gap VA28: volunteer and CERT roster"); deleting PA items; stale marking in the census; an incident picker on the screen; insurance amounts | verified | Basho: confirm the PA guide edition (decision 10); FEMA rate data (external) |
| F9 | Pre-disaster baseline data for damage assessment | Esri-EMO | Compare damage against a pre-event baseline | VEOC-23 proves imported jurisdiction baselines, approved assessments, loss totals and declaration output. Spatial exposure and statewide building-type coverage do not populate damage replacement-cost baselines. "V1 W3.12: screens for the remaining operator routes" puts the parcel baseline import from CSV or JSON on the Damage Assessment screen; "Veoci and air gap VA17: validated migration" keeps a report of each baseline import and replaces a parcel already in the baseline, which failed with 500 before (real-database tests). Remaining, per "V1 W3.2: damage assessment": live parcel-roll ingestion and statewide replacement-cost coverage need data acquisition, and baselines do not feed the declaration summary | partial | Basho: pilot data acquisition (external) |
| F10 | Always-on facility status networks + status queries | Juvare-other (EMResource) | Query facility status networks | Facility status + queries (VEOC-28); "V1 W3.4: facilities and shelters" adds the registry, status board, HAVE view and map; "V1 W3.13: screens for the optional integrations" adds status requests; "V1 W4.11: Public Assistance and shelter census" adds the shelter census from facilities reports. "Readiness RD9 part one: workflow, staffing and facilities" edits and retires registry entries, audited, keeping their reports' history, and lists the jurisdiction's status requests with who is still to answer. Facilities is an optional integration ("V1 W6.6: gated-module disposition"). Not claimed: a review for patient-level data | verified | - |
| F11 | Scan-first tracking objects + reunification | Juvare-other (EMTrack) | Scan a tag; track custody; reunify | Tracking + reunification (VEOC-25); "V1 W3.13: screens for the optional integrations" adds the custody chain. Tracking is an optional integration ("V1 W6.6: gated-module disposition"). Not claimed: restricted tracking details for cleared roles, which are hidden from every role; a review for patient-level data | verified | - |
| F12 | Incident templates instantiating ICS org + checklists | WebEOC-core | Activate an incident from a template with positions and checklists | Incident templates + checklists (VEOC-12); participation + authority added (`ca0d130`); "V1 W4.9: incident lifecycle" adds archival and the jurisdiction master view; "V1 79D+D33 part one: the integrated cross-boundary exercise" activates, draws the area with a period, onboards a partner, revokes and closes out across two organizations; "V1 79D+D33 part two: the integrated visual and accessibility review" makes the incident selector follow a new activation; "V1 W5.3: remaining interface findings" titles each activated board with the incident name and the template title, and "Readiness RD8: the screens on the open engineering list" retitles boards activated before that change, leaving renamed boards as they are; "Operator trust TP7: incident close and reopen" shows before a close what stays running and reopens a closed incident with a reason. "Veoci and air gap VA6: incident templates as data" (VC-01) authors and versions templates on Incident Setup, offers every stored template for activation and records the version an incident opened from; "Veoci and air gap VA7: activation notifies; people reached by group, position and shift" notifies chosen groups and positions in the activation's transaction; "Veoci and air gap VA12: the small EOC starter pack" and "Veoci and air gap VA16: the incident room" (VC-12) have activation also open the template's contact groups, reports, rules, dashboards, threads and file folders; "Veoci and air gap VA27: continuity of operations plans" adds a fourth standard template, Continuity of Operations. Each by real-database and component tests, and each but the starter pack by a browser test at both viewports. Not claimed: the standard position set has no EOC Director or Situation Unit Leader ("V1 W6.5: training kit"), though a template may now title positions the jurisdiction lacks (VA6); file folders are one level, made at activation, with no create, rename or move on screen | verified | - |
| F13 | Scenario/reference libraries, checklists | WebEOC-core | Attach scenario/reference libraries and run checklists | Libraries + checklists (VEOC-12), declarative due/category rules and attributed completion receipts (`ecd5249`), and assigned/offline task presentation (`d867488`). "V1 W3.12: screens for the remaining operator routes" puts Add a library, the incident's checklists and libraries, and Mark complete on the Incident Setup screen, with checklist completion walked in Chrome. "Veoci and air gap VA13: executable plans" (VC-09) releases a plan's tasks to positions on a schedule after activation and reminds administrators when a plan's review falls due; "Veoci and air gap VA30: corrective actions linked to plans" (VC-22) links an after-action corrective action to a plan's section and reminds its owner on the due date; both by real-database, component and browser tests (see G-PLANS). Not claimed: CBRNE or HAZMAT reference content, which does not ship; libraries are an attach-and-read mechanism, and checklist completion shows only on the signed-in position's items | verified | - |
| F14 | Calm-screen map-first SPA discipline | Esri-EMO | A map-first console that stays legible under stress | Map-first shell (VEOC-05/17), responsive operational frame (`ef13b0c`), persistent context (`64e3cf5`) and operational map workspace (`e20e772`) passed wide/narrow, light/dark and keyboard Chrome gates. "V1 79D+D33 part two: the integrated visual and accessibility review" runs axe on sign-in and twelve views in both themes at 1440, 390 and 720 wide with no serious or critical result, measures every keyboard ring at 3:1, and fixes 14 findings; "V1 A11Y-T1: reduced motion, higher contrast and the screen-reader script" honors reduced motion, higher contrast and forced colors; "V1 W5.0: code splitting" and "V1 W5.1: style consolidation" lighten the first load and steady the polling; "V1 W5.3: remaining interface findings" shows an error with a reload when a screen fails to load; "Readiness RD2 part three: the first review" fixes the seven things Basho's first review of the installed demo named, among them a Map screen that is the map and workspaces that scroll instead of clipping; "V1 CI stability: the deep-link race and the recurring red runs" and "V1 CI stability part two: a linked record outside the loaded rows" keep deep links. Remaining: representative-operator acceptance, whose absence is recorded in "V1 D34: operator workflow comparison (absence recorded)"; the NVDA and VoiceOver pass; D33 findings 15, 18, 21, 24 and 25 | verified | Basho: representative operators and the screen-reader pass (external); the D33 visual findings |
| F15 | Per-incident auto-provisioned collaboration space | WebEOC-core | Activating an incident provisions its workspace | Collab provisioning (VEOC-32/33); incident context now drives the workspace (`8c96103`); "V1 W3.13: screens for the optional integrations" puts channel setup, announcements, meeting bridges and briefings on screen; "V1 W2.12: network calls out of every write path" calls the collaboration backend with no transaction open. Collaboration and meetings are optional integrations ("V1 W6.6: gated-module disposition"). "Veoci and air gap VA4: collaboration and feeds in an outage" sends an announcement to the incident's position holders in the app when the configured chat server does not answer, answers channel setup and membership as degraded to run again later, and names the error on screen, by real-database tests with a failing transport; "Veoci and air gap VA16: the incident room" opens the incident's message threads at activation, in the product's own messaging. Not claimed: the screen does not show an incident's channel state; no run against a real Mattermost or Matrix server, and no browser walk against Matrix | verified | - |
| F16 | One-click role-based provisioning; ten-minute viewer path | WebEOC-core | Provision a jurisdiction and admin in one action | One-action provisioning (VEOC-08/41); "V1 W3.0: administration" puts provisioning, people, roles, positions and guest access on the Administration screen; "V1 W2.6: secure by default" adds the `bootstrap` command; the Windows setup runs it at a production profile's first setup ("V1 W6.4: installer rebuild"); the Docker path of "V1 W6.0: one-command server install" was removed with ADR-0010. "Veoci and air gap VA17: validated migration" (VC-13) adds a people import from a CSV or Excel file, checked row by row before anything is written, with accounts, memberships and positions, and a report signed off on screen, by real-database, component and browser tests; "Veoci and air gap VA18: rollout playbook and timed onboarding" (VC-14) writes the procedure for timing a person new to the product from install to first activation, with a result sheet. Not claimed: the ten-minute viewer path has not been timed; the timed cold-start run was deferred at VEOC-41 and only a dry run is recorded; the timed onboarding procedure has not been run, and its first run needs a person new to the product | verified | Basho: a person new to the product for the timed onboarding (a run) |
| F17 | Daily-ops usability against skill decay | WebEOC-core | Routine daily-ops use keeps skills fresh | Responsive, persistent daily-ops workspaces are integrated; "V1 W3 route coverage: every operator route owes a screen" and "V1 W3 milestone gate" put every operator route on a screen; "V1 W4.1 part two: board screen controls" adds the board controls; "V1 W6.5: training kit" adds position job aids and a tabletop exercise, and "Readiness RD11: the remaining checks" its inject cards. The Operator Trust units prove, as browser scenarios on the North Coast Storm exercise, an occasional operator's own work queue ("Operator trust TP2: my work"), a request's receipt, owner and next action ("Operator trust TP1: request lifecycle and findability"), a shift handoff ("Operator trust TP3: shift handoff") and work kept through interruptions ("Operator trust TP4: durable work and explicit state"). "Veoci and air gap VA18: rollout playbook and timed onboarding" adds a six-step rollout playbook for a small tribal or rural EOC and eight self-paced modules on the installed demonstration, checked against the screens' source and not yet walked by EOC staff. "Veoci and air gap VA39: job aids in the console" (the plan's amendment 1) opens the acting position's job aid from the console's **Help**, bundled in the web build so it opens with no connection, lists every aid with the nearest one named for a position that has none, and rewrites the eight aids to the screens as they are, this plan's features included, by unit, component (with axe) and browser tests at both viewports with an offline reload; the other aids' steps were checked label by label against the source, not walked. No representative operator has established skill-retention or comparative workflow results; the absence is recorded in "V1 D34: operator workflow comparison (absence recorded)" | partial | Basho: representative operators (external) |
| F18 | Sensor and drone live feeds into the COP | Esri-EMO | External hazard/position feeds appear as COP layers | CAP/GeoRSS/CoT/GeoJSON feeds retain staleness and failure reporting. VEOC-79C1/C2 deliver durable mapped items, tolerant refresh tallies and last-good preservation; P-COP provides layer/inspection presentation. "V1 W2.2: scheduler" polls feeds on the leader with no manual call, in both deploy paths (the desktop path checked by loading the module); "V1 W2.12: network calls out of every write path" fetches outside any transaction. "Veoci and air gap VA4: collaboration and feeds in an outage" raises one alarm per feed outage and closes it as recovered on the next success, and keeps a failing feed's last good items through retention however long the outage, by real-database tests with a failing fetch. Live outbound source evidence remains open | partial | Basho: a live source (external data) |
| F19 | NAPSG/DHS incident symbology shipped | Esri-EMO | Incident symbols render from a NAPSG set | Approved nine-type subset, original assets, local CC BY 4.0 license, reproducible sprites and independent status frames (`389afe1`) pass MapLibre light/dark legend, inspection and attribution proof and final P-COP composition; "V1 W4.5: operational vector tiles" draws the NAPSG icons on tile layers. Broad clinic/airport/EOC labels remain explicitly unmapped | verified | - |
| F20 | Native standards interchange | WebEOC-core / Esri-EMO | Import/export CAP, EDXL, CoT, GeoJSON natively | CAP/EDXL/CoT/HAVE/GeoJSON models and endpoints (VEOC-26..29/31); "V1 W3 route coverage: every operator route owes a screen" marks the interchange routes machine audience; "V1 W3.4: facilities and shelters" shows EDXL-HAVE on screen; "V1 W3.9: export and import" exports a jurisdiction as one archive; "V1 W4.4: WebEOC migration" reads WebEOC's CSV board export. "Veoci and air gap VA33: Esri interchange" (VC-26; threat row B19) serves every board whose map field the caller may read as a read-only ArcGIS FeatureServer (a service per board, a layer per geometry kind, object ids numbered per board, queries by object id and envelope in WGS 84 or Web Mercator, paged, answered as Esri JSON or GeoJSON), takes a service identity's token where Esri clients send it, reads Esri JSON feature sets in the board import with each shape checked by PostGIS, and lets a board's geometry field hold multipart shapes, by real-database, component (with axe) and browser tests at both viewports against the request shapes ArcGIS clients publish, and a loopback run of the shipped Caddy for its log. Not claimed: no ArcGIS Pro, ArcGIS Online or QGIS client has added a layer; edits through the view; a `where` other than `1=1`, statistics, sorting, `f=pbf` and `POST` queries; an ArcGIS sign-in (a client that asks for a user name and password is given the address with `?token=`); object ids across instances, which federation, exchange by file and the export do not carry; a federation peer on an earlier release refuses a record with a multipart shape until it is upgraded; a Road Closures board draws no areas imported into it; a hole that touches its area part-way along an edge may be refused as invalid | verified | Basho: add a board in QGIS, ArcGIS Pro and ArcGIS Online (a run); areas on a Road Closures board (decision) |

## Requirements (R1-R6)

| ID | Capability | Reference | Operator behavior | Local implementation / evidence | Status | Owner |
|---|---|---|---|---|---|---|
| R1 | >= 150 concurrent users per instance | WebEOC-core | 150 concurrent operators without degradation | In-process benchmark (VEOC-38). "V1 W2.4: WebSocket discipline" delivers every update to 149 live readers under 100 ms beside a stalled reader; "V1 W2 milestone gate" records a two-hour synthetic activation with 150 sockets, 71,106 edits and 0 errors, heap flat; the serial `load.test.ts` passed again in "V1 W4 milestone gate" and "V1 M5 milestone gate". "Readiness RD4: 150 people at once" runs 150 people, each from their own address, through HTTPS against the network host profile (PostgreSQL, the server and Caddy started as their services start them) for two hours after warm-up with every threshold met: 0 errors, reads 245 ms and writes 33 ms at the 95th percentile, a live edit reaching the others in 32 ms, heap growth 8.4 percent; it fixes the three sync defects the earlier runs found. Remaining, as that receipt states: the run used this machine's host profile, not an installed host, and the load generator shared the machine; the 95th-percentile read time rose from 127 ms to 399 ms over the run. "Readiness RD4 follow-up: reads that slowed as the incident filled" traced the rise to three reads that ran row-level checks once per row and fixed them: at the run's full volume the incidents overview went from 42.6 to 15.1 ms and thread messages from 38.8 to 9.0 ms, and a member's notification page from 295 to 11 ms at 60,000 notifications, by probe measurements against a real database and real-database tests. The two-hour rerun with the fix was skipped at Basho's instruction; the probe stands for it ("Version 0.9.2: the Windows setup"). The serial `load.test.ts` missed its 300 ms budget for a filtered board view over 50,000 records on the Linux bed at "Veoci and air gap phase VA-A gate", unexplained there, and passed 4 of 4 on Windows at "IPAWS connector IC3: documents". "V1 R1-REAL: the real-hardware 150-user run (boundary recorded)" records that no separate deployment hardware was supplied | partial | Basho: a run against the installed host, and the two-hour rerun with the read fix (external) |
| R2 | IPAWS integration, enable-at-will | WebEOC-core | Enable IPAWS alerting when authorized | IPAWS model/endpoint (VEOC-31). "V1 W2.7: threat-model controls" makes every send a two-person request the database refuses to let one admin confirm; "V1 W3.5: IPAWS enablement and send" adds configuration, the MOA acknowledgement, the enable toggle and the two-person send on screen, walked against a loopback fixture; "V1 W2.10: MFA" requires TOTP of every local admin account, and only admins can enable IPAWS; "V1 W2.12: network calls out of every write path" calls IPAWS-OPEN with no transaction open and never sends a confirmed request twice. "IPAWS connector IC1: the connector to the Interface Design Guide", "IPAWS connector IC2: the certificate on screen and each channel's answer" and "IPAWS connector IC3: documents" rebuild the connector to the IPAWS-OPEN Interface Design Guide v4.02.06: the alert and the SOAP request are signed with the COG certificate, which is checked before anything is signed, each channel's answer is recorded, and an answer the connector does not recognize is a rejection. Both signatures verify against the certificate in unit tests, and a browser walk runs against a loopback stand-in whose answers are shaped after the IDG's examples. Remaining: no send to IPAWS-OPEN, its test environment included, until the developer MOA and a COG certificate arrive | partial | Basho: IPAWS-OPEN credentials and the MOA (external) |
| R3 | Agency/organization/volunteer conglomerate COP access | WebEOC-inc | Multiple organizations share one incident's COP | Named participation, authority and guest access join incident-tagged records. PostgreSQL proves partner contribution/reads and outsider, revoked-grant and second-incident isolation; persistent incident URL state is integrated (`64e3cf5`). "V1 79D+D33 part one: the integrated cross-boundary exercise" runs the four open legs on real PostgreSQL with a second instance and walks owner and partner in two browser contexts: shared COP and impact counts, a partner field report queued offline and attributed after revocation, cross-organization resource requests, federation, the plan, revocation and closeout, with a second incident seeing none of it; "V1 79D+D33 part two: the integrated visual and accessibility review" fixes the three findings it carried; "V1 M5 milestone gate" fixes the walk's shelter wait. The partner sharing receipts (PS1 to PS5) share the incident's requests, positions, linked actions and incident-wide threads with every organization on it: "the partner requests and the owner assigns" works under Basho's request ownership decision, and the owner's record detail names a partner author's position and organization. "Operator trust TP6: partner invitations and recipient preview" sends each grant an in-app invitation naming the incident, position, role scope and expiry, shows whether it was read, previews under row-level security what the grant's person reads, and states that ending a grant does not recall what was delivered. "Veoci and air gap VA28: volunteer and CERT roster" lets a partner organization with a contributor grant enter its own volunteers for the incident and read only those, held by row-level security; "Veoci and air gap VA22: field breadth" sends a partner participant's work that reaches a closed incident to the owner's administrators as a late submission. Remaining: the peer attributes a federated batch to the sending instance, a bound "Readiness RD10: federation of record deletes and earlier records" leaves in place | partial | Basho, with F3: accept as a v1 limit or schedule it (open engineering) |
| R4 | Fluid Command and General Staff; JIC component | WebEOC-core | Staff hand off positions; JIC publishes | ICS staff + JIC (VEOC-12/33A/34); "V1 W3.8: JIC and resources completion" and "V1 W3.11: engine gaps the screens exposed" wire release review, publication, inquiries and a second approver's queue; "V1 W3.3: staffing" adds check-in, badges, shifts and the ICS-211; "Readiness RD9 part one: workflow, staffing and facilities" lists and revokes badges and makes the ICS-211 every check-in with its organization and check-out time; "Operator trust TP3: shift handoff" opens the briefing with the changes since the reader's last shift, the unresolved requests and overdue work with their owners, and the decisions awaiting action, each opening its source; "Veoci and air gap VA9 part two: QR codes on badges and pool resources" prints a badge's QR code and checks its holder in from a photo of the printed badge; "Veoci and air gap VA28: volunteer and CERT roster" (VC-20) keeps volunteers who hold no account (see G-VOLUNTEERS). Not claimed: an inquiry close step and publishing a release to CAP ("V1 W3.8: JIC and resources completion"); a list of unsubmitted JIC drafts, and a drafter's panel showing a decision recorded in another session ("V1 W6.5: training kit") | verified | - |
| R5 | File sharing | WebEOC-core | Upload and download files per jurisdiction | Content-addressed files (VEOC-15); tenant check + safe download hardened (`451ed16`); "V1 W2.6: secure by default" streams uploads with a size limit and a per-jurisdiction quota; "V1 W4.1 part one: board engine depth" applies a record's read rule to files attached to it; "V1 W4.7: field depth" uploads field photos and audio to their record after it synchronizes; "Veoci and air gap VA16: the incident room" files an upload in one of an incident's folders, made at activation and held to the incident by the database, by real-database and browser tests. Not claimed: folders are one level, and a stored file does not move between them | verified | - |
| R6 | Private and group messaging | WebEOC-core | Direct and group messages | Messaging (VEOC-15A/32); "V1 W3.12: screens for the remaining operator routes" adds thread export and message settings; "Veoci and air gap VA16: the incident room" opens an incident's threads at activation; "Veoci and air gap VA22: field breadth" keeps a message to an incident thread on a device with no connection and sends it once on return. Not claimed: message settings cannot be read back, which the same receipt defers; the Messages thread list shows the first page only ("V1 W2.11: remaining list pagination") | verified | - |

## Anti-requirements (AR1-AR7)

| ID | Anti-requirement | Guard | Local implementation / evidence | Status | Owner |
|---|---|---|---|---|---|
| AR1 | No per-seat surge pricing | INV-1 | Viewers structurally free; no seat metering | verified | - |
| AR2 | No unconstrained board divergence | INV-5 | Versioned board schemas with migration; "V1 W4 milestone gate" makes the designer read the board and its versions again after publish and apply | verified | - |
| AR3 | No admin customization requiring hand-written HTML/JS | INV-6 | Declarative authoring engine, runtime layouts, structured designer, preview, immutable template publication and safe board apply are integrated (`b7cb680`, `32249fc`, `75464fc`); "V1 W4.1 part two: board screen controls" adds the Record access and Local fields tabs; "Veoci and air gap VA15: workflow guards and per-state field permissions", "Veoci and air gap VA25: declarative action catalog" and "Veoci and air gap VA26: all-or-any conditions, date and text functions, every view option in the designer" add guards, per-state read-only fields, actions, condition groups and every view option, written with selects, checkboxes and plain text inputs only, with no expression or script input (the plan's decision 5; ADR-0004) | verified | - |
| AR4 | No in-place-upgrade dead ends | INV-5 | Versioned schema upgrades preserve records; "V1 W6.1: versioning and upgrade" forces a backup before every upgrade on both paths and records a restore drill. A database built before the 2026-09-22 migration baseline is refused, and no tool moves its data; records are carried across with the board import, as the [upgrade guide](./guides/UPGRADE.md) states. "Operator trust TP8: upgrades keep configuration" upgrades a database built through migration 0131 across every later migration and checks that local fields, layouts, dashboards, rules, positions and role grants survive with their capabilities, and the launcher writes a report of the migrations applied beside the pre-upgrade dump; "Readiness RD11: the remaining checks" has the setup stop running profiles before it replaces files. "Veoci and air gap VA19: signed peer identity" states one upgrade boundary: an existing federation peering receives nothing after the upgrade until both administrators record each other's public keys, as the federation guide says. Not claimed: a real upgrade of an installed copy | verified | - |
| AR5 | No proprietary-only interchange or substrate lock-in | INV-4, INV-9 | Native standards + jurisdiction export; Apache-2.0; "V1 W3.9: export and import" exports a jurisdiction's operational record and file bytes as one archive; "V1 W4.4: WebEOC migration" reads WebEOC's CSV board export; "Veoci and air gap VA10: the license gate" (VC-06) refuses fair-source and source-available licenses (FSL, Fair Core, PolyForm, Elastic-2.0, Camunda, Carbone and Open WebUI among them) and holds GPL, LGPL and MPL for a recorded review, by command tests against fixture packages; "Veoci and air gap VA32: OpenAPI document and scoped service identities" (VC-25) publishes an OpenAPI 3.1 description of every contract route, checked in as `docs/openapi.json`, held to the contract by the API document test and served to signed-in callers; "Veoci and air gap VA33: Esri interchange" (VC-26) serves boards to GIS clients as an ArcGIS FeatureServer and imports Esri JSON into a board | verified | - |
| AR6 | No session timeouts mid-incident, free-text drift, join-poor dashboards | INV-8 | Session continuity, controlled fields, server-computed dashboards; "V1 W4.10: progressive web app" keeps a saved session through a lost connection; "Operator trust TP4: durable work and explicit state" returns a console whose session the server ended to sign-in with the reason, and restores its drafts after sign-in. "Veoci and air gap VA34: offline device PIN for shared devices" adds a lock only under a device PIN, which a person chooses and is never required to set: 15 minutes without input locks the console until the PIN is entered, which resumes the session with its queued work; without a PIN nothing locks and nothing changes | verified | - |
| AR7 | Full function disconnected, including provisioning | INV-3 | Native Windows cold setup, local map, runtime RLS, profile isolation, durable work recovery and persistent restart are proven on this prepared machine (`3741100`, `7a52438`). "V1 W6.4: installer rebuild" builds the offline `0.9.0` setup (1,351,643,919 bytes, SHA-256 `dafddeb50fcdfdf9a85519d24a88e21ae1c87420357ad22d86927802b112a153`) carrying Node.js, PostgreSQL with PostGIS, the California street, Overture building and overlay archives and the address search gazetteer, and writes the second-machine transfer check. "Readiness RD5: the air gap" runs the network host with the demo through a Chrome walk of every rail section, the map, the notifications and a backup while recording every connection of every process the system starts: no connection left this computer and its local network, and no page request went outside (Chrome's own services reached eight Google hosts, listed apart as browser policy); it adds `Test-OpenEOCAirGap.ps1` for an unplugged run. "Version 0.9.2: the Windows setup" builds `Open-Source-EOC-Setup-0.9.2.exe` (SHA-256 `7ccd6a4dbe860395f781acc6a3aaae6909cbfcd438ec2f002c540f49755aecfd`) with the optional basemaps, and a ZIP of the same staged folder. The Veoci roster's continuity units: "Veoci and air gap VA1: hold, do not drop" keeps outbound messages through an outage shorter than their window; "Veoci and air gap VA3: private authorities and time" ships the unplugged check in the setup with a Start menu entry and adds clock checks to it; "Veoci and air gap VA20: exchange by file" carries a shared board across the gap on media; "Veoci and air gap VA22: field breadth" queues map points, messages and new tasks on a device with no network; "Veoci and air gap VA35: region map packs" builds a map packet for another area to carry in, checked by SHA-256 and installed with `InstallMapData`, by unit tests and a hand run with stand-in files. "Veoci and air gap plan gate: the air-gap proof with integrations on local stand-ins" adds `prove-airgap.mjs --stand-ins`: email, SMS (an HTTP provider, then a gateway phone), a webhook, an ntfy push, a feed and federation to a second host are configured against stand-ins on this computer, all cut before the walk and the backup and brought back after; on `2991317` (63 minutes, with a passing default run beside it) every delivery that waited went within 26 seconds of its route returning, the webhook left down past its one-hour window read expired and was resent, the feed read recovered, the partner received the record made during the cut, and no connection left this computer and its local network; the runs on the release commit belong to the plan-end gate. "Veoci and air gap VA24: the disconnected drill" (AG-10) writes Part 1, the unplugged run on two computers with the install from media, and Part 2, a 72-hour drill against local stand-ins, with a report template; AR7 and INV-3 close on Part 1's record. Not run, each a procedure not yet run and Basho's: Part 1 (the unplugged run with a second device and the setup carried on media to a disconnected second computer), Part 2, and a region packet built and carried in | partial | Basho: Part 1 of the disconnected drill; Part 2 and a region packet carried in (runs) |

## Assessed gaps beyond the canonical set (audit and amendment)

| ID | Capability | Reference | Local status / evidence | Status | Owner |
|---|---|---|---|---|---|
| G-INCSCOPE | Incident-scoped operational records (boards/feeds/resources/tasks) | WebEOC-inc | The 79B receipt closes scoped board writes/reads/counts, dashboards/live stream, COP, resources, tasks and IAP with database and DOM evidence. P-SHELL adds bounded incident URLs, selected-record resolution, invalidation and Back behavior (`64e3cf5`); "V1 W0.0: audit cleanup publication and M4" closes the long scoped-context browser gate; "V1 W4.9: incident lifecycle" adds per-incident lockdown at the row-level security wall; "V1 79D+D33 part one: the integrated cross-boundary exercise" shows a second incident seeing none of the first's records; "V1 79D+D33 part two: the integrated visual and accessibility review" has the board list carry each board's incidents, so another incident's boards leave the dock and layer list | verified | - |
| G-INGEST | Durable normalized dataset ingestion to the COP | Esri-EMO | VEOC-79C1/C2 deliver incident-RLS persistence, tolerant refresh counts, last-good preservation, GeoJSON items and available/stale COP layers; P-COP adds MapSurface inspection (`e20e772`). "V1 W2.2: scheduler" runs feed polls on the leader in both deploy paths; "V1 79D+D33 part one: the integrated cross-boundary exercise" loads a partner's dataset through the API as a feed push would and reconciles it on the map; "Operator trust TP5: information state you can read" names beside the map each registered dataset that has sent nothing or whose source failed; "Veoci and air gap VA4: collaboration and feeds in an outage" keeps a failing feed's last good items, shown stale, for the whole outage, with one alarm. Live outbound evidence remains open | partial | Basho: a live source (external data) |
| G-CATALOG | California operational data catalog + refresh controls | Esri-EMO | Coverage-aware catalog (boundaries/closures/parcels/hazards/facilities/population/shelters) onboards through the data-pack path with owner/license/coverage/mapping/cadence, listed per incident and added from the operator UI (`f34cd69`); "V1 W2.2: scheduler" runs scheduled polling. Named gaps are live FEMA NFHL, an open statewide shelter feed, live ACS and live outbound fetching | partial | Basho: FEMA NFHL, a statewide shelter feed and ACS (external data) |
| G-IMPACT | Incident-area impact analysis (affected structures/parcels/population) | Esri-EMO | 79G-E1/E2 provide authorized PostGIS totals, revision deltas, masked Lifeline reports and paged contributions. P-DASH presents source geometry and distinguishes missing, unknown, stale and measured zero in light/dark wide/narrow Chrome proof. "V1 W3.12: screens for the remaining operator routes" adds impact comparison; "V1 79D+D33 part one: the integrated cross-boundary exercise" reconciles the indicator (2, naming the partner's dataset) with the COP layer and a direct PostGIS count. Verified for loaded sources; no live ACS population baseline was acquired, so population exposure is not claimed | partial | Basho: ACS population baseline (external data) |
| G-PARCELS | Configurable parcel overlays | Esri-EMO | Handoff 10 reuses the catalog and dataset-COP path. A real-database scenario proves Humboldt coverage versus San Diego noncoverage, onboarding, APN mapping and polygon delivery; P-COP supplies shared popup/outline browser proof. "Readiness RD9 part three: address search and parcel tiles" draws parcel datasets from vector tiles at once, reading one page of items for the layer panel instead of up to 50,000 features, so a whole county draws. Other county sources and live outbound fetching remain gaps | partial | Basho: other county parcel sources (external) |
| G-FLOOD | Hazard hatching + FEMA flood zone overlays | Esri-EMO | Operational hatching and distinct A/AE/AO/shaded-X layers retain coverage, freshness and attribution. Paged retrieval and toggles pass PostGIS and light/dark MapLibre proof (`eb876ec`), composed in P-COP. "V1 W4.5: operational vector tiles" keeps flood datasets on paged GeoJSON, because their zone classification needs a pattern MapLibre expressions cannot express. Fixtures are synthetic; live NFHL acquisition remains unproven and catalog availability remains false | partial | Basho: live NFHL source (external data) |
| G-KPIMAP | Map-extent KPIs | Esri-EMO | Bounds callback, uncapped viewport intersections and P-COP KPI strip (`b4c14bf`) are integrated. PostgreSQL proves 1,001-record reconciliation beyond the client cap; Chrome proves movement, scope, revision/bbox-preserving drilldown and unknown/stale/unavailable/zero states. Light/dark wide/narrow captures retained. "V1 79D+D33 part one: the integrated cross-boundary exercise" reads 1 closed road in the incident and 0 in the second; "V1 79D+D33 part two: the integrated visual and accessibility review" makes the indicator strip a named focusable group | verified | - |
| G-BUILDINGS | Enriched building subtypes (Overture) | Esri-EMO | H14 installs 13,526,876 PMTiles features and exact OSM-way lineage enrichment from Overture 2026-08-19.0: 24,546 untyped features enriched, 728 known types retained, no duplicate footprints. MapLibre proves identity, status precedence, attribution and byte ranges in both themes; P-COP supplies final inspection. "V1 W6.4: installer rebuild" ships the enriched archive in the Windows setup. Not claimed: subtypes for footprints with no exact OSM-way match, which stay untyped and explicit | verified | - |
| G-BOARDROUTE | Configurable board workflow routing | WebEOC-DS | Declarative transactional execution, pinned definitions, current-authority approvals, due/escalation, immutable history and local notifications (`6c8dddc`) are configured and previewed through the structured no-code designer (`75464fc`); "V1 W3.7: workflow runtime in the record detail" runs transitions, approvals, due times, escalations and history from the record; "Readiness RD9 part one: workflow, staffing and facilities" adds reject and cancel with a note, spends the request's revision so its approvals never count toward a later request, and names each actor on the history route; "Veoci and air gap VA15: workflow guards and per-state field permissions" (VC-11) refuses a transition whose guard the record does not meet, with the reason shown before the button is pressed, checks the guard again at the last approval, and locks fields per state; "Veoci and air gap VA25: declarative action catalog" can request a transition when a record is created, a field changes or a state is entered; "Veoci and air gap VA26: all-or-any conditions, date and text functions, every view option in the designer" lets a guard hold on all or any of its conditions, with groups and day operators, and words an unmet group in the refusal. Each by real-database, component and browser tests. "Veoci and air gap follow-up: a workflow transition's lock order" has a transition take the record's incident lock before the board's, as record writes and the sync hub do, so a transition whose action writes the record and a write on the same incident no longer deadlock (real-database test). Not claimed: federation ingestion of a peer's records is not held to local states | verified | - |
| G-SHELL | Adaptable, responsive operator shell | Esri-EMO | Responsive frame, compact navigation, modal/focus boundaries and wide/narrow theme gates (`ef13b0c`) join incident-scoped persistent context, layouts, deep links and stale-response invalidation (`64e3cf5`); "V1 W5.0: code splitting", "V1 W5.1: style consolidation", "V1 W4.10: progressive web app", "V1 79D+D33 part two: the integrated visual and accessibility review", "V1 A11Y-T1: reduced motion, higher contrast and the screen-reader script" and both CI stability receipts extend it; "V1 W5.3: remaining interface findings" makes the context drawer a modal dialog on phone and tablet widths, saves only the docked drawer's state, announces loading notes and shows an error with a reload when a screen fails to load; "Readiness RD2 part three: the first review" adds the bell's notifications panel, the command bar's flush lists and account menu, and scrolling workspaces; "Operator trust TP4: durable work and explicit state" returns an ended session to sign-in with the reason; "Operator trust TP5: information state you can read" names the cause of a refused read and of a failed sign-in; "Veoci and air gap VA3: private authorities and time" shows a notice above the workspace when the device's clock and the server's differ by more than 30 seconds, by component and browser tests in both themes. Not claimed: which board list columns a phone keeps (D33 finding 21, Basho's); a panel or menu opened while the incident's workspace restores after sign-in closes when the console redraws ("Readiness RD2 part three: the first review") | verified | - |
| G-DASH | Composable saved dashboards + map/chart/list drilldown | Esri-EMO | Saved compositions, filter inheritance, scoped paged drilldowns and PostgreSQL-backed source geometry (`6aa5c09`) are presented in the operational dashboard workspace with light/dark and 1440/900/390 Chrome evidence (`7ce5da6`); "V1 W4.2: views beyond the list" adds kanban and calendar widgets; "Readiness RD8: the screens on the open engineering list" opens a kanban column's supporting records and a calendar item's record; "Veoci and air gap VA16: the incident room" makes a template's dashboards at activation, titled with the incident and listed first for it, with other incidents' dashboards left out; "Veoci and air gap VA31: charts in reports and create-record tiles" (VC-24) adds a create-record tile to a saved incident dashboard that opens the board's own form with preset values, disabled with the reason for someone who cannot write. Each by real-database, component and browser tests. Not claimed: an activated incident's dashboard shows, with no saved view chosen, under the screen's line "Legacy dashboard · not a saved incident overview", wording left for Basho; a tile's save is not queued on a device with no network | verified | Basho: the "Legacy dashboard" wording (decision) |
| G-MFA | MFA and SAML for IPAWS-authority accounts | WebEOC-core | "V1 W2.10: MFA" adds TOTP sign-in with single-use recovery codes, required for jurisdiction and instance admins, which covers every account that can enable IPAWS; enrollment, replay refusal and recovery are walked in Chrome. OIDC sign-in leaves the second factor to the identity provider. SAML is not built: the Finish PSPR's section 7 item 3 default is TOTP with no SAML until Basho names an identity provider that needs it | partial | Basho: identity-provider decision |
| G-TILES | Vector-tile path for operational layers past 1,000 features | Esri-EMO | "V1 W4.5: operational vector tiles" serves board and dataset layers as `ST_AsMVT` tiles under row-level security with field visibility, clusters points below zoom 12, and switches a board to tiles past one client page and a dataset past 50,000 features; PostgreSQL proves 5,000 points with an admin-only field absent from a member's tile, and Chrome proves all 5,000 records reach the map through authenticated tiles; "Readiness RD9 part three: address search and parcel tiles" draws parcel datasets from tiles and leaves a dataset feature narrower than two tile pixels out of its tile; "Veoci and air gap VA33: Esri interchange" puts the board tiles, the OGC items and the FeatureServer behind one read wall that also honours the map field's own read level, so a board whose location the caller may not read has no layer on any of them (before, its tiles and OGC items served the location), by real-database tests. Not claimed: flood datasets, which stay on paged GeoJSON; Find on map, Zoom to extent and building status coloring over more than a tile layer's first page; nested values in tile features; the last change of a record drawn from tiles, whose map inspector says freshness is unknown ("Operator trust TP9: from the map to the action") | verified | - |
| G-MIGRATE | WebEOC board record migration | WebEOC-core | "V1 W4.4: WebEOC migration" maps WebEOC's CSV board export onto a board, with a dry run that writes nothing, a saved mapping, a rejection CSV with the formula guard, WebEOC's bookkeeping columns kept in the creation audit, and repeat imports skipping rows already imported by dataid, on real PostgreSQL and in Chrome; the side-by-side run imports one record and rejects one; "Readiness RD8: the screens on the open engineering list" takes an Excel (`.xlsx`) export on the screen. "Veoci and air gap VA17: validated migration" (VC-13) keeps a report of every import that writes (the WebEOC migration, signed packages, parcel baselines, form imports and the new people import: counts, the mapping and each row's outcome with its reason), signed off once on screen by an administrator, and serves an import template per board headed by field keys with each enumerated field's allowed values; "Veoci and air gap VA17 completion: board record import reports" adds the board record import. Real-database, component and browser tests. Not claimed: processes, views, links and menus; value translation; latitude and longitude to a location; person, record-reference and attachment fields; incident tagging; updating a row already imported; linking rows by `prevdataid` | verified | - |
| G-GEOCODE | Offline address and place search | Esri-EMO | "V1 W4.6: offline address search" builds a gazetteer from the OpenStreetMap street archive with the Node standard library and serves search to signed-in people; the California build holds 7,636 places, 511,673 streets, 308,390 points of interest and 3,230,662 house numbers, median search 0.44 ms; "V1 W5.2 part two: county bounds from the bundle" serves county lookups from the bundle; "V1 W6.4: installer rebuild" ships the gazetteer in the Windows setup; "Readiness RD9 part three: address search and parcel tiles" adds reverse lookup (7 to 41 ms on the statewide gazetteer) with a "What is here?" map tool, reads a leading "St" as "Saint", and lets the builder name the containing city from a city boundary file. Not claimed: a merge with real county address points, none of which is on disk; the containing city in the shipped gazetteer, which is not rebuilt because no boundary file is on disk, so the nearest settlement is named; house numbers attached to the parcel or building that contains them, which needs parcel data with situs addresses | verified | Basho: a city boundary file, county address points and situs parcel data (external data) |
| G-INCLIFE | Incident archival, jurisdiction master view and per-incident lockdown | WebEOC-inc | "V1 W4.9: incident lifecycle" archives and unarchives closed incidents with an audit entry, lists every incident a jurisdiction owns with rollups under the reader's row-level security, and adds an opt-in lockdown that refuses guest reads of the incident's boards at the row-level security wall, proven on real PostgreSQL and in Chrome; "V1 W4.13: gaps the WebEOC side-by-side run found" closes live guest board sockets after a lock or a revocation; "Readiness RD8: the screens on the open engineering list" shows a console-wide banner while guest access is locked; "Readiness RD9 part two: resources" adds the `resource_requests(incident_id)` index; "Operator trust TP7: incident close and reopen" states before a close what stays running and reopens a closed or archived incident with a reason kept in the record of events; "Veoci and air gap VA22: field breadth" keeps work that reaches a closed incident as a late submission, which an administrator refuses with a reason or accepts after reopening. Not claimed: a guest socket that joins between the commit and the re-check stays open until it rejoins | verified | Basho: the lockdown default (guest reads only, opt-in) |
| G-PWA | Installable web app with an offline shell | Esri-EMO | "V1 W4.10: progressive web app" adds the manifest, icons and a service worker that precaches the shell, every screen's code and the bundled basemap (123 files, 5.89 MB), an update notice, and a session kept through a lost connection, walked in desktop Chrome online and offline; "V1 W6.4: installer rebuild" ships the icons so the worker installs from the Windows setup; "Readiness RD5: the air gap" opens the console with no connection after a restart, and lets the worker's first install take over before it copies the map files, so a reload offline opens the console once its own files are in. "Veoci and air gap VA22: field breadth" adds map points, messages and new tasks to what the app keeps offline, and "Veoci and air gap VA39: job aids in the console" the position job aids, whose browser test reloads the console offline and opens one. "Veoci and air gap VA23: phones on a host's authority" (AG-09) writes the procedure for trusting a host's authority and installing the app on an iPhone, iPad or Android device, with a record of the walk; documents checked against the host and web source, not walked on a device. "Veoci and air gap VA34: offline device PIN for shared devices" (VC-27; threat row B18) offers after each password sign-in, and in Settings, a device PIN that encrypts everything the device keeps for that person (AES-GCM under a random key, wrapped under one derived from the PIN by PBKDF2-SHA-256 at 600,000 iterations), seals the session beside it, asks for the PIN at start and after 15 minutes without input, starts a wait at the third wrong PIN in a row (30 seconds, doubling with each further one) and erases the person's copy, unsent work included, at the tenth, by unit, component (with axe) and browser tests at both viewports on the real build with its service worker. Not claimed: installing on a phone, a procedure not yet run; the sign-in page's **Trust this server** panel gives steps for Windows and macOS only (a finding VA23 carried here, not changed); without a PIN, the default, the device keeps each person's work and the last session unprotected, readable by anyone with the device, as before, which the console says; no jurisdiction can require a PIN; a copy of the storage lets a six-digit PIN be guessed away from the app in minutes, so disk encryption remains the defence for a lost device; store keys, vault names and wrong-PIN counts and viewer preferences are not encrypted; the PIN walked in Chromium only, not on a phone or in Safari or Firefox | partial | Basho: the phone walk (a run); whether a jurisdiction may require a device PIN (decision) |
| G-REPORT | Saved reports with totals, PDF, Excel and CSV output and schedules | WebEOC-core | "V1 W4.3: reporting" saves report definitions over one board with grouping and totals, renders PDF, Excel and CSV, and schedules runs through the scheduler with email and Files delivery, on real PostgreSQL and in Chrome; the side-by-side run's tasks 15 to 17; "V1 W4.13: gaps the WebEOC side-by-side run found" shows choice labels on the screen, PDF and Excel; "V1 W5.1: style consolidation" keeps every heading on a phone. "Veoci and air gap VA1: hold, do not drop" sends a scheduled report's email through the delivery queue, retried until its hold window ends and resendable after; "Veoci and air gap VA31: charts in reports and create-record tiles" (VC-24) adds a bar or donut chart by a field, or bars over time per hour, day or week, counting the report's own records, drawn in the preview, in the PDF as vector pages before the table and as counts in Excel and CSV, by real-database, component and browser tests. Not claimed: runs over 50,000 records, which answer 413; one chart per report, counting records, with no sums or lines; any-of conditions and day values in the report builder, which stays all-of and counts days in UTC ("Veoci and air gap VA26: all-or-any conditions, date and text functions, every view option in the designer", its landing note) | verified | - |
| G-DR | Backup, upgrade and disaster recovery | WebEOC-core | "V1 W6.1: versioning and upgrade" forces a complete backup before any upgrade, makes a restore refuse an incomplete dump, and records a restore drill of 13,831 rows in about 3 seconds; "V1 W6.2: disaster recovery runbook" sets recovery point and time objectives, schedules a daily backup with retention and gives off-computer copy guidance. The Docker path those receipts also covered was removed with ADR-0010; the runbook and upgrade guide cover Windows. "Readiness RD3: the Windows network host" adds the host's daily backup task, whose command ran against a running host and whose backup restored into a new database with equal counts, without the services installed; "Operator trust TP8: upgrades keep configuration" writes an upgrade report beside the pre-upgrade dump; "Readiness RD11: the remaining checks" builds a scheduled backup's file store copy from hard links and has the setup stop running profiles before it replaces files. macOS is not started (G-MACOS). Evidence is unit, real-database, a host run without the system changes, and document. Not claimed: a real upgrade, scheduled backup or restore of an installed copy; undelete or partial restore | partial | Basho: first real run on an installed Windows workstation and host (external) |
| G-A11Y | WCAG 2.1 AA accessibility | Esri-EMO | "V1 79D+D33 part two: the integrated visual and accessibility review" (axe with no serious or critical result on sign-in and twelve views in both themes at 1440, 390 and 720 wide, keyboard rings at 3:1, 14 findings fixed); "V1 A11Y-T1: reduced motion, higher contrast and the screen-reader script"; "V1 W5.3: remaining interface findings" (tab panels, the drawer's dialog role, announced loading notes). Not claimed: the manual NVDA and VoiceOver pass; D33 findings 15, 18, 21, 24 and 25, assigned to Basho; the map canvas is visual, and its features are reached through the find box, layer list and inspector | partial | Basho: the screen-reader pass (external) |
| G-OPS | Structured logs and metrics | WebEOC-core | "V1 W2.8: observability" writes structured JSON request logs with request ids and redaction, serves Prometheus metrics behind a token, and rotates logs in both deploy paths; "V1 W2.2: scheduler" adds leader and job metrics; "Veoci and air gap VA33: Esri interchange" logs a request without its query string, and has the Windows host's Caddy log replace a `token` query value and the acknowledgement link's token and drop the credential headers a proxy error used to write, proved against the shipped Caddy on loopback | verified | - |
| G-HOST | Windows network host with HTTPS, and connecting to it | WebEOC-core | "Readiness RD3: the Windows network host" adds the setup's Host for the network choice: PostgreSQL, the server and Caddy as LocalService services, HTTPS from a certificate authority the host creates, a firewall rule, a daily backup task and `Test-OpenEOCHost.ps1`, proven by an automated run of the host without the system changes; "Readiness RD7: connecting to a host, on Windows" opens the installed app on a host, names why a connection fails, and shows the authority's thumbprint to compare, checked in Chrome and Edge against a loopback host; "Readiness RD4: 150 people at once" and "Readiness RD5: the air gap" run on the host profile. "Veoci and air gap VA3: private authorities and time" (AG-06) has the server trust the Windows certificate store and an agency authority file for its connections out, proven at process level against an SMTP stand-in on a second private authority, and adds clock lines to `Test-OpenEOCHost.ps1` and `Test-OpenEOCAirGap.ps1`, run against an NTP stand-in under PowerShell 7.4.6; "Veoci and air gap follow-up: trusted package keys on a Windows install" trusts package publishers from `trusted-template-keys.pem` in the host's profile folder (unit test); "Veoci and air gap VA23: phones on a host's authority" writes the phone steps. The `0.9.2` setup carries the host ("Version 0.9.2: the Windows setup"). Not run: installing the services, firewall rule, task and trusted root on a real machine, which changes system settings and is Basho's; the two check scripts' clock lines on Windows; the phone walk; Safari and the Mac side of Connect, which wait on G-MACOS | partial | Basho: the installed host and `Test-OpenEOCHost.ps1` (external) |
| G-MACOS | macOS workstation, network host and demo | WebEOC-core | "Readiness RD6: macOS, not started" left RD6 open, and it stays open under the Operator Trust PSPR. Since then "Version 0.9.2: the macOS build and its map data packet" ports the launcher's workstation and demo to macOS and adds a "macOS demo" workflow that builds `Open Source EOC.app` with its own PostgreSQL and PostGIS on a Mac runner; "CI repairs after the Actions runs resumed" records that workflow's run 36175150836 building the disk image, whose smoke test started the app's own PostgreSQL and answered ready. "Version 0.9.2: the macOS disk image, built on Windows" builds `Open-Source-EOC-0.9.2-macOS.dmg` (SHA-256 `39a03370f66ad335869c316ef3ae8721527a7a1819d3f43f66bc0cebbfe8ee14`) on Windows, read back file by file, without PostgreSQL inside: it uses Postgres.app from Applications. The maps come in the map data packet (SHA-256 `a8131b050fdc42109bfe940dafa4d072b0b6e634fb56c54263002eb554536727`), which the launcher checks and installs. The CI's macOS job runs only when started by hand. Evidence: a runner's smoke test, unit tests and a read-back of the built image. Not run: anything by a person on a Mac (opening the image, Gatekeeper's first-open prompt, the demo with Postgres.app, a packet installed from Downloads). Not built: a network host on a Mac (the host stays on Windows) and a signed app | partial | Basho: the first Mac run and an Apple Developer ID (external); open engineering: RD6 and the Mac host |
| G-PLANS | Executable plans: incident response, recurring event and continuity of operations, with corrective actions | Veoci | "Veoci and air gap VA13: executable plans" (VC-09) keeps versioned plans on Incident Setup whose sections name an incident template's positions, boards, contact groups and rules; activation opens the incident, releases timed tasks to positions on the scheduler (a recurring event's from its start), sends the plan's notice through the delivery queue, and review reminders go to administrators; "Veoci and air gap VA13 and VA14 corrections: review findings" corrects the review date and the activation report. "Veoci and air gap VA27: continuity of operations plans" (VC-19) adds continuity plans: essential functions opened as tasks in priority order, each due within its recovery time, with recovery locations, orders of succession and delegations of authority, a Continuity of Operations standard template and a starting plan for a small tribal or rural government that names no place or person. "Veoci and air gap VA30: corrective actions linked to plans" (VC-22) links a corrective action to a plan's section, reminds its owner once on its due date, and lists a plan's open actions when it is read. Each by real-database, component (with axe) and browser tests at both viewports. Not claimed: a plan does not activate itself on a calendar (a recurring event plan is activated per occurrence); plans are not in the jurisdiction export; recovery locations are text, not map features; business impact analysis and risk scoring (VC-30, outside the plan); due dates are read by the UTC calendar | verified | - |
| G-PACKAGES | Signed solution packages and ready-made content | Veoci | "Veoci and air gap VA11: signed solution packages" (VC-07) adds a package format signed with Ed25519 that carries board, incident, form, dashboard, report and rule templates, imported in one transaction after the signature is checked, a version already held counted as already here and a conflicting one refusing the whole package, with `new-package-key` and `sign-package` commands and an import on the designer's Import tab; "Veoci and air gap VA11 follow-up: the imported packages on screen" lists what an instance imported. "Veoci and air gap VA12: the small EOC starter pack" (VC-08) ships an any-hazard activation (nine positions, seven boards, 29 checklist items, contact groups, reports and rules) and a tabletop template, and "Veoci and air gap VA16: the incident room" adds its dashboard, threads and folders. "Veoci and air gap VA29: hotline and shelter registration pack" (VC-21) ships a hotline and inquiry log and shelter registrations, whose record rule keeps viewers and guests out, with no field for a diagnosis, condition or medication. "Veoci and air gap follow-up: trusted package keys on a Windows install" has the launcher and the host's service trust the publisher keys in `trusted-template-keys.pem` in the profile folder. Evidence: each pack signed and imported on a fresh profile and activated with no other setup, a changed package refused, on real PostgreSQL; the import screen by component and browser tests (VA11); the trusted keys by a unit test. Not claimed: the packs ship unsigned, to be signed with the deploying organization's own key; positions and contact groups come only through an incident template; a locally edited incident template is kept rather than replaced. Not run: the signed starter pack imported on an installed Windows computer with the key in place, and the starter pack's tabletop. The packs' content is Basho's to judge. A jurisdiction viewer on an incident reads fields marked for members on the incident's board screen (VA29's finding, a decision for Basho) | partial | Basho: the signed import on an installed computer (a run); the packs' content; member-marked fields on an incident's board screen (decision) |
| G-VOLUNTEERS | Volunteer and CERT roster | Veoci | "Veoci and air gap VA28: volunteer and CERT roster" (VC-20) keeps people who are not accounts (CERT teams, faith groups, partner organizations, community groups) with skills and credentials with issue and expiry dates; contacts sit in their own table, which viewers, partner viewers and guests do not read, and audit payloads leave them out; a partner organization with a contributor grant enters its own volunteers for the incident; a deployment names the credentials its role needs and warns on a missing or expired one before saving; hours are counted per volunteer per local day from ended deployments, merged where they overlap. By real-database, unit, component (with axe) and browser tests at both viewports. Not claimed: volunteer hours as Public Assistance donated resources, which the receipt leaves for Basho; a credential's history (a past deployment's warning reads today's credentials); hours from deployments still under way; the screen with no network, since it is not in the offline outbox | verified | Basho: donated resources credit (decision) |
| G-SERVICE | Scoped service identities for integrations, and a machine-readable API description | Veoci | "Veoci and air gap VA32: OpenAPI document and scoped service identities" (VC-25; threat row B17): a jurisdiction administrator issues a service identity at viewer or member level, never administrator, with an expiry of at most 366 days; its token is shown once and only its SHA-256 is kept, matched inside the database; it acts through a backing row with one membership, so the service layer and row-level security hold it as they hold a person with that membership, and its writes are audited under `<name> (service identity)`; a viewer identity is refused every method but GET and HEAD; it stops when revoked, when it expires, or when its creator is no longer an enabled administrator of the jurisdiction, for background work too; ten person-only routes refuse it; failed tokens alone count toward a per-address backoff. An OpenAPI 3.1 description of every contract route, with request schemas for 26 routes, is `docs/openapi.json` and `GET /api/v1/openapi.json`. An independent adversarial review found three medium and five low issues, each fixed with a test. By real-database tests (row-level security alone among them), component tests with axe and browser tests at both viewports. "Veoci and air gap VA33: Esri interchange" lets the five FeatureServer routes also take a service identity's token from `X-Esri-Authorization` or a `token` query parameter, checked the same way, and never a person's session from either. Not claimed: the WebSocket channels, which take a person's session only, so a wall display on a dashboard's live stream needs a person signed in; response bodies, query strings and the other request bodies in the description; a paged list of identities | verified | Basho: a service identity on a dashboard's live stream (decision) |

## Reconciliation notes

- This reconciliation ("Veoci and air gap VA36: the documents reconciled",
  completed in "Veoci and air gap VA36 completion: VA33, VA34 and the
  follow-ups") checked every row against the receipts through `98f050a` on
  2026-09-25, for the planned `v0.9.9` in
  [RELEASE-DECISION.md](../RELEASE-DECISION.md): every unit of the Veoci
  Integration and Air Gap PSPR, VA1 to VA39, with their follow-ups and
  corrections and the plan gate's air-gap proof with integrations on local
  stand-ins, the IPAWS connector units IC1 to IC3, "Readiness RD4 follow-up:
  reads that slowed as the incident filled", the "Version 0.9.2" receipts,
  the CI repairs and the CI corrections. The previous one ran through
  `4ccad8e` ("Operator Trust landing: the full gate") earlier the same day.
  Each row cites its evidence by receipt heading and states what is not
  claimed.
- The completion changes no status. VA33 adds evidence to F20, AR5, G-TILES,
  G-OPS and G-SERVICE; VA34 to F7, AR6 and G-PWA; the follow-up that runs
  board actions on edits through sync closes F1's not-claimed item for those
  edits; the lock-order follow-up adds to G-BOARDROUTE; the plan gate's
  stand-ins run adds to AR7. F20, still `verified`, now names Basho's run of
  the Esri clients and his decision on areas on a Road Closures board; G-PWA
  names his decision on requiring a device PIN.
- Status changes: G-MACOS moves from `open` to `partial` (the Mac app and
  its disk image are built; no person has run them on a Mac). New rows
  G-PLANS, G-VOLUNTEERS and G-SERVICE (`verified`) and G-PACKAGES
  (`partial`), and the Veoci reference key. The roster's receipts close
  not-claimed items in F4 (inbound SMS acknowledgement, now text replies
  through a gateway on the site network), F5 (objectives and the ICS-208 on
  screen, a 205 with content), F7 (offline queueing for a board with record
  rules, on an incident) and G-REPORT (charts; retry of a scheduled send);
  narrow the remaining boundary of F3, R1, AR7, G-PWA and G-HOST; and add
  evidence to F1, F2, F8, F9, F12, F13, F15, F16, F17, F18, R2, R3, R4, R5,
  R6, AR3, AR4, AR5, G-INGEST, G-BOARDROUTE, G-SHELL, G-DASH, G-MIGRATE and
  G-INCLIFE. F4, F8, F16 and G-DASH, still `verified`, now name an owner for a
  decision or a run the receipts leave to Basho.
- Evidence level of that work. Each unit ran its own real-database,
  component and browser tests and its neighbouring suites, as each receipt
  records; VA18, VA23 and VA24 are documents checked against the source, and
  VA35 is unit tests with a hand run on stand-in files. The phase gate for
  VA1 to VA5 ran on the Linux bed with five browser tests failing there and
  the load test unexplained ("Veoci and air gap phase VA-A gate"); no phase
  gate is recorded for phases VA-B, VA-C or VA-D. RD5's proof with every
  optional integration a stand-in can play passed on `2991317` ("Veoci and
  air gap plan gate: the air-gap proof with integrations on local
  stand-ins"), and a trial `pnpm check:gate` on `f94d6ee` failed 2 of 380
  files, both fixed since, its serial Vitest run 2,025 passed and 2 failed of
  2,030 tests ("Veoci and air gap follow-up: Vitest collected a node test
  file"); both run again on the release commit. The plan-end gate is the
  integrator's; its results go in section 6 of
  [RELEASE-DECISION.md](../RELEASE-DECISION.md). Hosted CI runs `pnpm check`
  on Windows on each push to `main`; the last recorded result is run
  36216221581 on `055792e`, 3 of 2,030 tests failed ("CI correction: a time
  zone named by its alias, and a partner's message check", which fixes the
  two time zone cases and makes the third name its cause if it recurs). No
  receipt records a green hosted run. Until the plan-end gate is recorded, a
  `verified` status that rests on this plan's units means their own suites
  passed, not the full serial gate.
- The procedures this plan wrote are not evidence of a run: the timed
  onboarding (VA18), the phone walk (VA23), Parts 1 and 2 of the
  disconnected drill (VA24) and a region map packet carried in (VA35) are
  each Basho's to run, and a row that waits on one says so.
- On the earlier reconciliation of 2026-09-25 no existing row changed status.
  The Readiness and Operator Trust receipts close not-claimed items in F1, F4, F7, F10, F12, R4,
  G-BOARDROUTE, G-DASH, G-GEOCODE, G-INCLIFE, G-MIGRATE, G-PWA and G-TILES;
  narrow the remaining boundary of F3, R1, AR7, G-PARCELS and G-DR; add
  evidence to F2, F5, F6, F8, F14, F17, R3, AR4, AR6, G-INGEST and G-SHELL;
  and add rows G-HOST and G-MACOS. "Operator trust TP0: corrections to the
  platform research" corrects the research document and changes no row.
- The units behind that evidence ran one full `test:ci` together, with the
  failed files run again after their fixes, and each unit's static checks
  ("Operator Trust landing: the full gate"). The phase gates
  (`pnpm check:gate`) and a second full run were not run. Hosted CI now runs
  its check job on Windows ("Readiness RD12 part one: CI on Windows"); no
  receipt records its first run's result.
- The W4 gate rows F1, F4, F7, F8, F13 and G-TILES stay `verified`.
- `partial` rows and their owners: F3 (incident records, a v1 limit for Basho
  to accept or a design for Basho to choose; the drill's day-2 exchange),
  F9, F18, G-INGEST, G-CATALOG, G-IMPACT, G-PARCELS and G-FLOOD (external
  data), F17 (representative operators), R1 (a run against the installed
  host; the two-hour rerun with the read fix), R2 (the developer MOA and a
  COG certificate), R3 (federated batch attribution, with F3; Basho's request
  ownership decision is made and carried out by the partner sharing plan),
  AR7 (Part 1 of the disconnected drill; Part 2 and a region packet),
  G-MFA (Basho's identity-provider decision), G-PWA (the phone walk; whether
  a jurisdiction may require a device PIN), G-DR (a
  first real run of an installed copy), G-A11Y (the screen-reader pass),
  G-HOST (the installed host), G-MACOS (the first Mac run; RD6 and the Mac
  host) and G-PACKAGES (the signed import on an installed computer; the
  packs' content; member-marked fields). No row is `open`.
- G-IMPACT and G-BUILDINGS carried labels that are not statuses ("verified for
  loaded sources", "verified for exact-way enrichment"). G-IMPACT is now
  `partial` for the missing population baseline; G-BUILDINGS is `verified`
  with the unmatched footprints stated as not claimed.
- New rows G-MIGRATE, G-GEOCODE, G-INCLIFE, G-PWA, G-REPORT, G-DR, G-A11Y and
  G-OPS record capabilities the Finish PSPR added.
- Citation correction: F1 and AR3 cited `48edf63` for the operational record
  workspace. That commit was the baseline the work started from; the work
  landed as `32249fc`.
- No row claims a capability the code does not support; unproven depth is left
  `open` or `partial`, never asserted as parity. No design milestone is
  represented as whole-product parity.
- Live readiness (R1 on an installed host, R2 live IPAWS, AR7 independent
  transfer, named external datasets and the pilot) is not converted into proof
  by this documentation reconciliation. Each is an external input for Basho.
- AR7 stays `partial` after "V1 W6.4: installer rebuild", "Readiness RD5:
  the air gap" and the Veoci roster's continuity units: the setup exists,
  its contents match its stage manifest, and the recorded run on this
  machine made no connection outside it and its local network, but no setup
  has been installed from media on a disconnected second computer and
  Basho's unplugged run is not recorded. Both are Basho's external actions,
  now joined in Part 1 of the [disconnected drill](./guides/DISCONNECTED-DRILL.md)
  ("Veoci and air gap VA24: the disconnected drill"); the row moves on its
  record.
