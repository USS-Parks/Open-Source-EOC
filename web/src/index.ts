/**
 * @openeoc/web
 * React front end. The design system arrives in VEOC-05; until then this
 * package holds only the workspace wiring proof.
 */

import { workspaceInfo } from "@openeoc/shared";

export const webInfo = workspaceInfo("@openeoc/web");
